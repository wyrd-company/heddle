---
heddle: minor
heddle-feature: minor
---

Read and write the kanban board's task files directly, so the service needs no `kanban-md` on `PATH`, pins no build, and checks no version. Repository scope becomes an ordinary front-matter property, and any `kanban-md` that preserves properties it does not own interoperates with the same board.
