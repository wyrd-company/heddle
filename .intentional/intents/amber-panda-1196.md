---
heddle: minor
---

Publish Heddle to npmjs as the public package `@wyrd-company/heddle` with npm provenance on `heddle@*` tags. The package declares the supported Node range (`^24.0.0 || ^26.0.0`) and documents a command-line installation path. The GitHub release asset is no longer produced.
