---
heddle: minor
heddle-feature: patch
---

Use conventional worker paths and service endpoints when deployment configuration omits them; keep explicit overrides and internalize the loopback listener host.
