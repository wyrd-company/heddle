---
heddle-feature: minor
---

Install the published `@wyrd-company/heddle` package from a configurable npm registry. `version` follows the registry `latest` dist-tag by default or installs an exact version; new `npmRegistry` option, credentials in its URL are refused. The Feature source moves to `features/heddle/`.
