# Replace this ordinary policy artifact to map the project's issue types.
# An issue that matches no rule is held by the intake blueprint until it does.
rules:
  - id: work-item
    when: type = 'Work item'
    blueprint: standard-lifecycle
    inputs: {}
