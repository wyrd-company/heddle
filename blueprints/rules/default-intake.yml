# Replace this ordinary policy artifact to map the project's issue types.
rules:
  - id: work-item
    when: type = 'Work item'
    blueprint: standard-lifecycle
    inputs: {}
  - id: wait-for-type
    blueprint: hold-then-attention
    inputs:
      expectedType: Work item
