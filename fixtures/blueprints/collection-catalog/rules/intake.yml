# Intake selection remains replaceable policy data.
rules:
  - id: catalog
    when: type = 'Collection request'
    blueprint: collection-catalog
    inputs: {}
  - id: hold
    blueprint: hold-then-attention
    inputs:
      issue: { from: "$" }
      expectedType: Collection request
